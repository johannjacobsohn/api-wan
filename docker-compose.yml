services:
  frontend:
    build:
      context: .
      dockerfile: apps/frontend/Dockerfile
      args:
        VITE_SWAPI_BASE_URL: ${VITE_SWAPI_BASE_URL:-https://swapi.info/api}
    restart: unless-stopped
